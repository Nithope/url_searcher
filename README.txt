Tecnical Test

As a Software Analyst, I want to collect web links (URL’s) from a given initial web link (URL)

Requirements:

The app needs to receive an URL;:green_check_mark:
The app needs to find all links inside this given URL;:green_check_mark:
The app needs to save this links found in database (SQL or No-SQL):green_check_mark:
The app needs listing this links saved in the database.:green_check_mark:
After collecting all links from the initial URL. Collect from the newly found links. I mean, :green_check_mark:
the system gets the first link saved, and start the process (get all links and keep on the database).:green_check_mark:
Follow does it to the second, third and successively until the last link saved and tracked.:green_check_mark:

Extra

Please, as an extra effort, include in your solution the following:

Your code needs to be integrated with git on your GitHub personal profile;:green_check_mark:
Your code needs to contain unit testing;:green_check_mark:
Your code needs to be served on the IBM Cloud;:green_check_mark:
Your code needs to run with containers, with in IBM Cloud; :green_medium_square:
